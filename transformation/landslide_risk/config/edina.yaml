# Landslide risk (H×E×V) — Edina, MN
site_slug: edina
display_name: Edina

hazard_tif: ../landslide_hazard/sites/edina/data/output/landslide_hazard_score_edina_90m.tif
acs_ev_gpkg: ../acs_ev/sites/edina/data/output/acs_ev_block_groups.gpkg

exposure_field: exposure_score
vulnerability_field: vulnerability_score

# Publish (COG / XYZ tiles / S3 / catalog)
s3_prefix: oef_calculation/release/v1/edina/climate_hazards/landslides
shared_s3_prefix: oef_calculation/release/v1/edina/shared
tile_zoom: "8-15"
publish:
  tile_zoom: "8-15"
